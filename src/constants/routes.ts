/**
 * 🐸 UI ROUTES
 *
 * Central place for all application URLs.
 * Use these in your Page Objects instead of hardcoding strings.
 */
export const Routes = {
  home: '/',
  login: '/login',
  example: '/example',
} as const;
